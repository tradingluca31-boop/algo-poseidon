# Dépendances Python pour l'enrichissement des données ML
# Installation: pip install -r requirements.txt

yfinance>=0.2.28
pandas>=2.0.0
numpy>=1.24.0

# Indicateurs techniques (choisir l'un des deux)
# Option 1 (recommandé si disponible):
# TA-Lib>=0.4.28

# Option 2 (fallback si TA-Lib ne compile pas):
pandas-ta>=0.3.14
